version: '3.7'

services:
  db:
    image: postgres:12.2
    restart: always
    environment:
      POSTGRES_USER: test 
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ikproj
    ports:
      - 5432:5432